# Result


```bash
ello, World
```

# fix

- Fixed. Changed `s` from `char *` to `String_View` type and used the `sv()` constructor. The program now outputs `ello, World` (chopped the "H" from "Hello, World").

